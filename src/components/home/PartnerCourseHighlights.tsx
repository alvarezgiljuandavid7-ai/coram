import { useEffect, useState } from 'react';
import { Handshake } from 'lucide-react';
import { Link } from 'react-router-dom';
import { affiliateRepository, type AffiliateCourse } from '../../domain/affiliates/affiliateRepository';
export function PartnerCourseHighlights(){const[courses,setCourses]=useState<AffiliateCourse[]>([]);useEffect(()=>{affiliateRepository.listPublished().then((items)=>setCourses(items.filter((item)=>item.featured).slice(0,2))).catch(()=>undefined);},[]);if(!courses.length)return null;return <section><div className="flex items-end justify-between gap-3"><div><p className="text-[10px] font-black uppercase tracking-[.2em] text-[#a56b09]">Aliados</p><h2 className="font-serif text-3xl text-[#17305a]">Formación recomendada</h2></div><Link className="text-sm font-bold text-[#3d7146]" to="/app/academia/aliados">Ver todas</Link></div><div className="mt-4 grid gap-3 md:grid-cols-2">{courses.map((course)=><Link key={course.id} to={`/app/academia/aliados/${course.id}`} className="flex items-center gap-4 rounded-[1.3rem] bg-white p-4 shadow-[0_10px_24px_rgba(24,45,71,.06)]"><span className="grid h-12 w-12 shrink-0 place-items-center rounded-2xl bg-[#fff0d1] text-[#a56b09]"><Handshake/></span><span><small className="font-black uppercase text-[#a56b09]">{course.partnerName}</small><strong className="block text-[#0B2545]">{course.title}</strong><em className="mt-1 block text-xs not-italic text-[#667085]">Recomendación afiliada</em></span></Link>)}</div></section>}
